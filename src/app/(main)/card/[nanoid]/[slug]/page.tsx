import MainBodyHeader from "@/components/layouts/main-layout/components/main-body-header";

export default function CardDetails({
  params,
}: {
  params: { nanoid: string; slug: string };
}) {
  return (
    <div>
      <MainBodyHeader title="">132</MainBodyHeader>
    </div>
  );
}
